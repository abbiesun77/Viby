# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Viby 托管 AI（OpenAI 兼容接口）
# 用户用完 Credit 后可在设置中切换到自己的 Key（BYOK）
VIBY_AI_BASE_URL=
VIBY_AI_API_KEY=
VIBY_AI_TEXT_MODEL=gpt-4o
VIBY_AI_IMAGE_MODEL=gpt-image-2

# Supabase Storage：需创建一个名为 project-assets 的 public bucket
# 用于存放上传替换的参考图
